export interface UserPhoto {
  filepath: string;
  webviewPath?: string;
}

export interface Beer {
  id: string;
  name: string;
  ibus: string;
  degrees: string;
  style: string;
  capacity: string;
  origin: string;
  description: string;
}
